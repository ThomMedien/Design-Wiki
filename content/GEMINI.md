# Workspace Mandates

- **Syncing:** After making any changes to the directory, always ensure you run the sync command. 
  - Procedure: `cd .. && npx quartz sync` (assuming the current directory is `content`).
