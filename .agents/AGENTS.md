# Project Rules

- Whenever you modify any files in the workspace (create, edit, or delete markdown files or configuration files), you must run `npx quartz sync` immediately after to sync the changes and make them live.
